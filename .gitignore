node_modules
dist
.vite
*.log
.DS_Store

# Brainstorming companion (visual mockups + state)
.superpowers/

# DSP test artifacts (regenerated on every test run)
test/output/

.playwright-mcp/

# Playwright artifacts (regenerated on every e2e run)
test-results/
playwright-report/

# Ad-hoc dev screenshots & captured command output dropped in the repo root
/*.png
/*.txt

# Claude Code transient worktrees
.claude/worktrees/

# Claude Code personal/local settings (not shared with the team)
.claude/settings.local.json

# Personal editor config (e.g. CMake settings from sibling C++ work) — not shared
.vscode/

# Harvested raw assets (mixed-license / copyright) — not committed; loaded from public/ at runtime
drum-samples/
midi-library/
public/drumkits/**/*.wav
loops/
