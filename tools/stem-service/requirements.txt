fastapi
uvicorn[standard]
audio-separator
python-multipart
librosa

# Melodic transcription: Spotify basic-pitch (neural polyphonic AMT) run on ONNX.
# basic-pitch pins TensorFlow + an old numpy that will NOT build on Python 3.12 /
# numpy 2, so install it WITHOUT its deps and provide its pure-python runtime deps
# (below) — it then uses the already-present onnxruntime:
#     pip install basic-pitch --no-deps
pretty_midi
mir_eval
onnxruntime

# Drum transcription: ADTOF-pytorch (a CRNN trained on 359h of real music, 5-class
# kick/snare/hi-hat/toms/cymbals). Torch-only (no TensorFlow/Keras/madmom) and the
# weights are bundled, so it installs cleanly on Python 3.12 and runs on the torch
# already present for Demucs:
#     pip install git+https://github.com/xavriley/ADTOF-pytorch.git
