// One knob from strict periodicity to apparent chaos, over an ORDINAL rather
// than over time. The SDK's, so the host and `plugins/pernote` cannot end up
// disagreeing about what Pattern 0.618 means.
export { patternValue, patternValueBipolar, GOLDEN_PATTERN } from '@loom/plugin-sdk';
